<?php

// Mautic test config template for CI/Docker environments.
// Copy to config/local.php in your Mautic installation.

$parameters = [
    'db_driver'   => 'pdo_mysql',
    'db_host'     => getenv('DB_HOST') ?: '127.0.0.1',
    'db_port'     => getenv('DB_PORT') ?: '3306',
    'db_name'     => getenv('DB_NAME') ?: 'mautic_test',
    'db_user'     => getenv('DB_USER') ?: 'root',
    'db_password' => getenv('DB_PASSWORD') ?: 'mautic',
    'db_table_prefix' => '',
    'secret_key'  => 'test_secret_key_not_for_production',
    'site_url'    => 'http://localhost',
];
